{
  "name": "customer-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "build": "esbuild src/handlers/*.ts --bundle --platform=node --target=node20 --outdir=dist --format=esm",
    "test": "echo 'No tests'",
    "deploy": "npm run build && terraform -chdir=terraform apply -auto-approve",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^29.5.3",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "ajv": "^8.12.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "husky": "^9.0.11",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.573.0",
    "@aws-sdk/lib-dynamodb": "^3.573.0",
    "ajv-formats": "^2.1.1",
    "uuid": "^9.0.1"
  }
}
