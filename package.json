{
  "name": "customer-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "build": "node esbuild.mjs",
    "deploy": "npm run build && terraform apply -auto-approve -input=false"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.598.0",
    "@aws-sdk/client-dynamodb": "^3.598.0",
    "@aws-sdk/lib-dynamodb": "^3.598.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-prettier": "^5.1.3",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "esbuild": "^0.20.2",
    "aws-sdk-client-mock": "^3.1.0"
  }
}
