{
  "name": "customer-api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "test": "jest",
    "build": "esbuild src/handlers/*.ts --bundle --platform=node --target=node20 --format=esm --outdir=dist --external:@aws-sdk/*",
    "deploy": "npm run build && terraform apply -auto-approve -input=false"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.544.0",
    "@aws-sdk/client-dynamodb": "^3.544.0",
    "@aws-sdk/lib-dynamodb": "^3.544.0",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1",
    "aws-lambda": "^1.0.7",
    "aws-xray-sdk-core": "^3.5.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/jest": "^29.5.4",
    "@typescript-eslint/eslint-plugin": "^7.7.0",
    "@typescript-eslint/parser": "^7.7.0",
    "esbuild": "^0.20.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.29.1",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.0"
  }
}
